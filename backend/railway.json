{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '[CMD] container up'; node -v; echo '[CMD] dist contents:'; ls -la dist || echo '[CMD] NO dist DIR'; echo '[CMD] migrating...'; npx prisma migrate deploy; echo \"[CMD] migrate exit=$?\"; echo '[CMD] launching node (unbuffered)...'; node dist/server.js; echo \"[CMD] NODE EXITED code=$?\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
